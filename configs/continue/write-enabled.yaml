mcpServers:
  filesystem-controlled:
    command: npx
    args:
      - "-y"
      - "@modelcontextprotocol/server-filesystem"
      - "{WORKSPACE_ROOT}"
  git-controlled:
    command: uvx
    args:
      - mcp-server-git
      - "--repository"
      - "{WORKSPACE_ROOT}"
