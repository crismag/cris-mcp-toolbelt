# Generic MCP governance-summary template — format reference.
#
# This is the neutral YAML format produced by scripts/render_config.py for
# `--client generic` / `--client generic-yaml`. It intentionally records
# catalog references, capability classes, and default modes rather than client
# runtime blocks.
#
#   python3 scripts/render_config.py --profile <profile> --client generic \
#       --output {TARGET_REPOSITORY}/{MCP_CONFIG_DIR}/cris-mcp-toolbelt.yaml
#
# Use `--client continue` for real Continue-compatible MCP server blocks.

version: 1
generated_by: cris-mcp-toolbelt render_config.py
client: generic-yaml
profile: readonly-research
mcpServers:
  - ref: public-fetch
    capability_class: interactive
    mode: readonly
  - ref: sequential-thinking
    capability_class: passive
    mode: enabled
