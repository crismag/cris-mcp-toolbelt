name: validate

on:
  pull_request:
  push:
    branches: [main]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: python -m pip install --upgrade pip pyyaml jsonschema pytest

      - name: Validate catalog
        run: python3 scripts/validate_catalog.py

      - name: Validate profiles
        run: python3 scripts/validate_profiles.py

      - name: Policy lint
        run: python3 scripts/policy_lint.py

      - name: Doctor
        run: bash scripts/doctor.sh

      - name: MCP server smoke checks
        run: bash scripts/test_mcp_servers.sh

      - name: Shell syntax check
        run: |
          for s in scripts/*.sh; do
            bash -n "$s"
          done

      - name: Python syntax check
        run: |
          for p in scripts/*.py; do
            python3 -m py_compile "$p"
          done

      - name: Run test suite
        run: python3 -m pytest tests/ -q
