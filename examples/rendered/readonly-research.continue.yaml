# Generated by cris-mcp-toolbelt render_config.py — do not edit by hand.
# Regenerate with:
#   python3 scripts/render_config.py --profile readonly-research --client continue --dry-run
mcpServers:
  github-controlled:
    command: docker
    args:
      - run
      - -i
      - --rm
      - -e
      - GITHUB_PERSONAL_ACCESS_TOKEN
      - ghcr.io/github/github-mcp-server
    env:
      GITHUB_PERSONAL_ACCESS_TOKEN: '{GITHUB_TOKEN}'
  public-fetch:
    command: uvx
    args:
      - mcp-server-fetch
  sequential-thinking:
    command: npx
    args:
      - -y
      - '@modelcontextprotocol/server-sequential-thinking'
