# Ollama Local AI Setup

This toolbelt supports local-first AI workflows.

## Example

- Runtime: `ollama`
- Model: any locally available model
- Policy: keep sensitive data local whenever possible

Use a read-only profile first, then promote only trusted actions to write-enabled workflows.
