# Data Analytics MCP Guidance

Default posture for analytics/data MCP integrations:

- Use read-only queries by default
- Avoid direct write/DDL/DML permissions in standard profiles
- Scope credentials and datasets minimally
- Require explicit approval for write-enabled profile usage
