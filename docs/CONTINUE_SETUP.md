# Continue Setup

1. Pick a profile (`readonly-research`, `sandbox`, or `writer`).
2. Run workspace enablement script.
3. Open your Continue MCP config in the target repo:

`{TARGET_WORKSPACE}/.continue/mcpServers/cris-mcp-toolbelt.yaml`

4. Verify only expected servers are enabled for that profile.
