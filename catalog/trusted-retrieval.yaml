id: trusted-retrieval
name: Trusted Retrieval MCP
category: online-retrieval
description: Example trusted provider server limited to retrieval workflows.
provider: Example Provider
source_url: https://example.com/mcp/trusted-retrieval
trust_level: trusted
default_access: read-only
profile_allowlist:
  - readonly-research
  - writer
security_notes:
  threats:
    - Data exfiltration through prompts
  mitigations:
    - Read-only default
    - Provider trust review
    - Profile-gated escalation
